# Token del bot de Telegram (lo da @BotFather al crear el bot).
TELEGRAM_BOT_TOKEN=

# chat_id al que enviar los mensajes. Se obtiene con el tool
# `obtener_chat_id_pendiente` (ver README) y se rellena aquí después.
TELEGRAM_CHAT_ID=
